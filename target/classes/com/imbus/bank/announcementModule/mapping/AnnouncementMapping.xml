<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.imbus.bank.announcementModule.dao.AnnouncementDao">
    <select id="getAnnouncementNumber" resultType="java.lang.Integer">
<<<<<<< HEAD
        SELECT COUNT(id) FROM tb_announcement
        <where>
            title LIKE '%${title}'
            userName LIKE '%${userName}'
        </where>
    </select>
=======
    SELECT COUNT(id) FROM tb_announcement
    <where>
        title LIKE '%${title}'
        userName LIKE '%${userName}'
    </where>
</select>
>>>>>>> 69424e0c6c6c7fc126748c31336a9f38527e5901

    <insert id="addAnnouncement">
        INSERT INTO tb_announcement
        <set>
            title = #{title}
            userID = #{userID}
            content = #{content}
        </set>
    </insert>

    <update id="editAnnouncement">
        UPDATE tb_announcement
        <set>
            title = #{title}
            userName = #{userName}
            content = #{content}
        </set>
        <where>
            id = #{announcementID}
        </where>
    </update>

    <delete id="deleteAnnouncement">
        DELETE FROM tb_announcement
        <where>
            id = #{announcementID}
        </where>
    </delete>

    <select id="getAnnouncementDetail" resultType="com.imbus.bank.announcementModule.bo.AnnouncementBo">
        SELECT * FROM tb_announcement
        <where>
            id = #{announcementID}
        </where>
    </select>

    <select id="getAnnouncementList" resultType="com.imbus.bank.announcementModule.bo.AnnouncementBo">
        SELECT tb_user.userName,tb_announcement.* FROM tb_announcement INNER JOIN  tb_user ON tb_announcement.userID=tb_user.userID
        <where>
            title LIKE '%${title}'
        </where>
    </select>

</mapper>